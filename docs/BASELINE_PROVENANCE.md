# Baseline provenance

The exact organiser starter was not retrieved, so this repository does **not**
claim to reproduce the organiser baseline.

`reference_lgbm` is an independently selected, regularised LightGBM reference
used to make local experiments comparable. Its metadata records:

- `organiser_parity_status`: `UNAVAILABLE`
- source URL: unavailable
- organiser version: unavailable
- retrieval date: unavailable
- source hash: unavailable

This status must remain `UNAVAILABLE` until an exact, attributable starter is
retrieved and its source, version, date, and hash are recorded.
