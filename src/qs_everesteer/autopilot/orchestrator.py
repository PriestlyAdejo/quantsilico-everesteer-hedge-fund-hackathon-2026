class CompetitionAutopilot:
    """Deterministic orchestrator; LLM hypotheses are optional structured inputs."""

    def run(self, profile: str = "competition_aggressive"):
        raise NotImplementedError
